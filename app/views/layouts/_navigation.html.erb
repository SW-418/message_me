<div class="ui secondary pointing menu">
    <%= link_to "Chatroom", root_path, class: "item" %>
    <a class="item">
    Messages
    </a>
    <div class="right menu">
        <% if logged_in? %>
            <%= link_to "Logout", login_path, method: "delete", class: "item" %>
        <% else %>
            <%= link_to "Login", login_path, class: "item" %>
        <% end %>
    </div>
</div>
